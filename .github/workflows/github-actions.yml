name: CI
on:
  push:
    branches: [main]

jobs:
  checkout-and-test:
    runs-on: ubuntu-latest
    container:
      image: donaldm314/ubuntu-bats:28
    steps:
      - uses: actions/checkout@v4.1.7
      - name: "Test"
        run: |
          task
          ls -lart
          id
